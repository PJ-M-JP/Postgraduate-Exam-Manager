# 考研管家 · 跨平台安装包

本目录包含「考研管家」在三端（iOS / 鸿蒙 / 安卓）的安装包。每个子目录是**自包含**的，可以单独打包分发（zip / 上传网盘 / 发给朋友）。

## 快速选你的设备

| 设备 | 进入目录 | 安装方式 |
| --- | --- | --- |
| iPhone / iPad | `考研管家-iOS/` | Safari → 分享 → 添加到主屏幕 |
| 华为 / 荣耀（鸿蒙） | `考研管家-鸿蒙/` | 华为浏览器 → 添加到桌面 |
| 安卓（华为/小米/OPPO/vivo/三星） | `考研管家-安卓/` | Chrome → 安装应用 / 打包 APK |

## 通用说明

- **App 本体**位于 `app/` 目录，是一份纯静态网页（HTML/CSS/JS），三端共享同一份代码
- **数据本地存储**于浏览器 `localStorage` + `IndexedDB`，关闭重启数据不丢
- **可选云同步**：在 App 内登录 Supabase 账号即可多端同步（配置见 `app/supabase_schema.sql`）
- **离线可用**：首次联网一次后，无网也能打开

## 工作目录结构

```
Study/                                  ← 项目根（你在这里）
├── app/                                ← App 本体（6 模块 49 功能）
│   ├── index.html                      ← 入口
│   ├── manifest.webmanifest            ← PWA 清单
│   ├── sw.js                           ← 离线 Service Worker
│   ├── css/style.css
│   ├── js/                             ← 6 个模块 + 数据层 + 同步层
│   ├── icons/                          ← 跨平台图标（PNG/SVG）
│   ├── vendor/                         ← pdf.js 本地化
│   └── supabase_schema.sql             ← 云同步建表脚本
│
├── 考研管家-iOS/                       ← iOS 安装包
│   ├── 启动.html                       ← iOS 安装引导页
│   └── README.md
│
├── 考研管家-鸿蒙/                      ← 鸿蒙安装包
│   ├── 启动.html                       ← 鸿蒙安装引导页
│   └── README.md
│
└── 考研管家-安卓/                      ← 安卓安装包
    ├── 启动.html                       ← 安卓安装引导页
    ├── README.md
    ├── 打包APK.bat                     ← Windows 打包助手
    └── 打包APK.sh                      ← macOS / Linux 打包助手
```

## 怎么用

1. 决定你的设备平台，进入对应子目录
2. 双击 `启动.html`（或用对应浏览器打开）
3. 按页面上的步骤操作，30 秒内把 App 安装到主屏幕
4. 之后完全离线可用

## 分发建议

把单个平台目录（如 `考研管家-安卓/`）整个压缩成 zip 发给用户，对方解压后双击 `启动.html` 即可走安装流程。`启动.html` 里的图标和 App 链接都是相对路径，整包可整体搬运。
